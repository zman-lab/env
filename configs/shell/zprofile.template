# ============================================================
# .zprofile - 로그인 시 한 번만 실행되는 설정
# ============================================================

# Homebrew 초기화
eval "$({{BREW_PREFIX}}/bin/brew shellenv)"

# 작업 디렉토리 생성
[ ! -d "$HOME/work" ] && mkdir -p "$HOME/work"
